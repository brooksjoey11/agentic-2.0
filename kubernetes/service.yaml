apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: agentic-shell
spec:
  selector:
    app: orchestrator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
